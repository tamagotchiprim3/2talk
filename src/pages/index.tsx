import Chat from "./components/chat/chat";

const ChatPage: React.FC<{}> = ({}) => {
  return (
    <div className="w-full h-full bg-teal-950">
      <Chat />
    </div>
  );
};

export default ChatPage;
